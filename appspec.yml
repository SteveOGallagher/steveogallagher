version: 0.0
os: linux
files:
  - source: /
    destination: /var/www
permissions:
  - object: /var/www
    pattern: "*.sh"
    mode: 755
    type:
      - file
hooks:
 ApplicationStop:
   - location: stop.sh
     timeout: 1000
 ApplicationStart:
   - location: start.sh
     timeout: 1000
